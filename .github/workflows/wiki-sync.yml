name: "Wiki Sync"

on:
  push:
    branches: [main]
    paths: ["Wiki/**"]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    name: Sync Wiki
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Clone Wiki repo
        run: |
          git clone "https://x-access-token:${{ secrets.WIKI_SYNC_TOKEN }}@github.com/bayraktarozcan/Brave-Omega-Project.wiki.git" "$GITHUB_WORKSPACE/wiki-sync"
      - name: Sync to Wiki repo
        run: |
          rsync -a --delete "Wiki/" "$GITHUB_WORKSPACE/wiki-sync/"
          cd "$GITHUB_WORKSPACE/wiki-sync"
          git config core.autocrlf false
          echo "*.md text eol=lf" > .gitattributes
          find . -name '*.md' -exec sed -i 's/\xEF\xBB\xBF//g' {} +
          find . -name '*.md' -exec sed -i 's/\r$//' {} +
          git config user.name "bayraktarozcan"
          git config user.email "bayraktarozcan@users.noreply.github.com"
          if [ -n "$(git status --porcelain)" ]; then
            git add -A
            git commit -m "sync: update Wiki from main (${{ github.sha }})"
            git push origin master
          else
            echo "No changes detected — wiki already in sync"
          fi
